# 🆘 خطوات تشغيل قاعدة البيانات (Firestore)

المشكلة الحالية التي تظهر لك: `The database (default) does not exist`.
وهذا يعني أن "المكان" المخصص لتخزين البيانات لم يتم إنشاؤه بعد في مشروع Firebase الخاص بك.

## ✅ الحل (خطوات بسيطة جداً):

1. افتح هذا الرابط: [Firebase Console](https://console.firebase.google.com/)
2. اضغط على اسم مشروعك: **qisa-caffee**.
3. من القائمة الجانبية:
   - اضغط على **Build** (أو "بناء").
   - ثم اختر **Firestore Database**.
4. ستجد زرًا بمنتصف الصفحة بعنوان **Create Database**، اضغط عليه.
5. **⚠️ خطوة هامة جداً:**
   - ستظهر نافذة تسألك عن إعدادات الأمان (Security Rules).
   - تأكد من اختيار **Start in test mode** (البدء في وضع الاختبار).
   - *هذا سيسمح للبرنامج بالعمل فوراً دون تعقيدات.*
6. اضغط **Next** (التالي) ثم **Enable** (تفعيل).
7. انتظر قليلاً حتى يتم إنشاء قاعدة البيانات.
8. ارجع لصفحة الكاشير واعمل تحديث للصفحة (**Refresh**).

---
*بمجرد عمل هذه الخطوات، ستختفي رسالة الخطأ وسيتم الاتصال بنجاح! 🚀*
